{
  "name": "gakumas-contest-planner",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:backend": "bun --filter backend dev",
    "dev:frontne": "bun --filter frontend dev"
  }
}
